
[module/network]
type = internal/network
interface = wlo1

interval = 1.0
accumulate-stats = true
unknown-as-up = true

format-connected = <label-connected>

format-disconnected = <label-disconnected>
format-disconnected-foreground = ${wal.alternate}
format-disconnected-background = ${wal.background}

label-connected = "%{A1:networkmanager_dmenu &:} %essid% %{F-} %downspeed%%{A}"
label-disconnected = "%{A1:networkmanager_dmenu &:}睊%{A}"

label-connected-foreground = ${wal.primary}
label-disconnected-foreground = ${wal.alternate}

label-connected-background = ${wal.background}
label-disconnected-background = ${wal.background}
